FROM nginx:1.25-alpine

# Install envsubst (part of gettext for variable substitution)
RUN apk add --no-cache gettext

# Copy the nginx config template from the config directory
COPY config/nginx.conf.template /etc/nginx/nginx.conf.template